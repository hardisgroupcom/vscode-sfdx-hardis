/* eslint-disable */
// LWC: ignore parsing errors for import/export, handled by LWC compiler
// @ts-nocheck
// eslint-env es6
import { LightningElement } from "lwc";

/**
 * Cloudity logo mark, theme-aware: path colors come from SLDS palette tokens
 * so the mark stays readable in both light and dark VS Code themes.
 * Size is controlled by the parent (the svg fills its container width).
 */
export default class CloudityLogo extends LightningElement {}
